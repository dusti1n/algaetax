# algaetax; file: blacklist.txt
# Defines terms to exclude from taxon names during filtering (e.g. "sp", "cf", "complex")
# Blacklist for algaetax: one term per line, case-insensitive, '#' = comment
# Use lowercase for clarity (e.g. sp, cf, complex)
spec
spec.
cf
cf.
nr
nr.
gran
sonst
sonst.
komplex
complex
unb
